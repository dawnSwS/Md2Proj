[package]
name = "md2proj"
version = "1.0.0"
edition = "2021"
description = "Extract code blocks from AI generated Markdown files and auto-generate project files."

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
regex = "1.12.4"